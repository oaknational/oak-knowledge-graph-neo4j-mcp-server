# Environment variables for Neo4j MCP Server deployment

# Neo4j Database Configuration (Required)
NEO4J_URI=neo4j+s://your-neo4j-instance.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-neo4j-password

# Cloud Run Configuration (Managed by Cloud Run)
PORT=8080

# Server Security Configuration (Optional)
# Comma-separated list of allowed hostnames for Host header validation
# Defaults to "localhost,127.0.0.1" if not set
# Example: ALLOWED_HOSTS=your-service-name-hash.region.run.app,localhost,127.0.0.1
ALLOWED_HOSTS=

# CORS (Cross-Origin Resource Sharing) Configuration (Optional)
# Controls which websites can make browser requests to your MCP server
# Defaults to "*" (allow all origins) if not set
# NOTE: CORS only affects browser requests, not direct API calls (curl, Postman, etc.)
# Example: ALLOW_ORIGINS=https://my-frontend.com,https://app.example.com
ALLOW_ORIGINS= 
